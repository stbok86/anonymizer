# Release Notes - Anonymizer v1.5

## 🎯 Основные исправления

### Исправлена критическая проблема дублирования UUID
- **Проблема**: Одинаковый UUID назначался нескольким вхождениям одного текста
- **Решение**: Реализована позиционная логика для уникальной замены каждого вхождения

### Улучшена логика замены в таблицах
- Добавлена проверка позиций для точного попадания в нужную ячейку
- Реализован механизм раннего выхода после успешной замены
- Исправлена обработка множественных вхождений в одной таблице

### Улучшена логика замены в параграфах  
- Добавлена позиционная проверка для параграфов
- Исправлена замена только первого вхождения в каждом run
- Реализован раннее завершение после успешной замены

### Добавлено выделение замененных значений
- UUID в анонимизированных документах теперь выделяются желтой заливкой
- Выделение применяется автоматически ко всем типам замен

## 🧪 Тестирование

Добавлены комплексные тесты:
- `test_real_positions.py` - тест с реальными позициями для таблиц
- `test_paragraph_positions.py` - тест позиционной логики для параграфов  
- `test_same_element_multiple_uuids.py` - тест множественных замен в одном элементе
- `analyze_all_tables.py` - анализ структуры таблиц в документах

## 🔧 Технические детали

### Обработка позиций
- Замены обрабатываются в обратном порядке по позициям для корректного сдвига
- Реализована проверка позиций с допуском ±100 символов для таблиц
- Для параграфов используется менее строгая проверка позиций

### Алгоритм замены
1. Сортировка замен по позиции (в обратном порядке)
2. Поиск точного вхождения по позиции
3. Замена только первого найденного вхождения
4. Применение выделения к замененному тексту
5. Раннее завершение для предотвращения множественных замен

## ⚡ Производительность
- Оптимизирована логика поиска и замены
- Уменьшено количество лишних итераций через документ
- Улучшена обработка больших документов с множественными заменами

## 🐛 Исправленные баги
- Исправлен баг с дублированием UUID для одинакового текста
- Исправлена логика замены в многострочных параграфах
- Исправлена обработка текста, разделенного на несколько runs
- Исправлена проблема с неразрывными пробелами в тексте

## 📝 Совместимость
- Полная обратная совместимость с предыдущими версиями
- Никаких изменений в API
- Автоматическое применение новой логики ко всем существующим документам