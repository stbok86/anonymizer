# 📋 Детекция полных названий информационных систем

## 🎯 Общий принцип работы

Детекция полных названий информационных систем — это самый сложный алгоритм в нашей системе. Он должен найти и правильно разделить длинные названия типа "Единая информационная система управления финансово-хозяйственной деятельностью" на:
- **Общую часть** (остается открытой): "Единая информационная система" 
- **Приватную часть** (заменяется на UUID): "управления финансово-хозяйственной деятельностью"

## 🔧 Основные компоненты

### 1. **Регулярные выражения (Regex-паттерны)**
```python
patterns = [
    'единая_ис_exact': r'(?i)(Единая)\s+(информационная)\s+(система)',
    'единая_ис_declined': r'(?i)(едино[йыеми])\s+(информационно[йыеми])\s+(систем[аыеуой])',
    'государственная_ис': r'(?i)(государственна[яоейыми])\s+(информационна[яоейыми])\s+(систем[аыеуой])',
    'автоматизированная_ис': r'(?i)(автоматизированна[яоейыми])\s+(информационна[яоейыми])\s+(систем[аыеуой])'
]
```

**Что делают:**
- Ищут базовые конструкции типа "Единая информационная система"
- Учитывают разные падежи (единой, единую, единая, etc.)
- Поддерживают разные типы систем (государственная, федеральная, автоматизированная)

### 2. **Извлечение дополнительного контекста**
После нахождения базового паттерна система ищет **продолжение** названия:

**Метод `_extract_system_suffix()`:**
- Берет текст после найденного паттерна
- Продолжает читать слова до стоп-слов
- **Стоп-слова**: "предназначена", "используется", "содержит", "обеспечивает", etc.
- Останавливается на знаках препинания

**Пример:**
```
Текст: "Единая информационная система управления кадрами предназначена для работы"
Базовый паттерн: "Единая информационная система" 
Продолжение: "управления кадрами" (до стоп-слова "предназначена")
```

### 3. **Умное разделение (Intelligent Partitioning)**
**Метод `_intelligent_partition()`:**
- **Общая часть**: базовый паттерн в оригинальном падеже
- **Приватная часть**: специфическое описание области применения

## 🚀 Пошаговый алгоритм

### Шаг 1: **Поиск базовых паттернов**
```python
# Ищем в тексте паттерны вида "Единая информационная система"
match = re.search(pattern, text)
core_text = match.group()  # "Единая информационная система"
```

### Шаг 2: **Извлечение продолжения**
```python
remaining_text = text[match.end():]  # Текст после найденного паттерна
suffix = _extract_system_suffix(remaining_text)  # "управления финансами"
```

### Шаг 3: **Формирование полного названия**
```python
if suffix:
    full_name = f"{core_text} {suffix}"  # "Единая информационная система управления финансами"
else:
    full_name = core_text  # Только базовая часть
```

### Шаг 4: **Разделение на части**
```python
core_part = core_text        # "Единая информационная система" (остается)
private_part = suffix        # "управления финансами" (заменяется)
```

### Шаг 5: **Создание анонимизированного текста**
```python
if private_part:
    anonymized = f"{core_part} {uuid.uuid4().hex[:8]}"  # "Единая информационная система A1B2C3D4"
else:
    anonymized = core_part  # Остается без изменений
```

## 🎯 Примеры работы

### Пример 1: Простое название
```
Вход: "Единая информационная система работает стабильно"
Найдено: "Единая информационная система"
Общая часть: "Единая информационная система"
Приватная часть: (отсутствует)
Результат: "Единая информационная система" (без изменений)
```

### Пример 2: Сложное название
```
Вход: "Единая информационная система управления кадрами предназначена для HR"
Найдено: "Единая информационная система управления кадрами"
Общая часть: "Единая информационная система"  
Приватная часть: "управления кадрами"
Результат: "Единая информационная система 4F8A9B2C"
```

### Пример 3: Склонения
```
Вход: "Подсистема единой информационной системы содержит данные"
Найдено: "единой информационной системы"
Общая часть: "единой информационной системы" (сохраняем падеж!)
Приватная часть: (отсутствует)
Результат: "единой информационной системы" (без изменений)
```

## ⚙️ Ключевые особенности

### 1. **Сохранение падежей**
Система **не нормализует** падежи — если в тексте "единой информационной системы", то так и остается.

### 2. **Интеллектуальные границы**
Система знает, где заканчивается название:
- По стоп-словам ("предназначена", "используется")
- По знакам препинания
- По смысловым границам

### 3. **Многоуровневая защита**
- Базовые названия (ЕИС, ГИС) остаются открытыми
- Специфические детали заменяются на UUID
- Контекст остается понятным

### 4. **Высокая точность**
- Уверенность: 0.8 (80%)
- Метод: 'information_system_regex'  
- Точное определение позиций в тексте

## 🔍 Диагностика

Для отладки система выводит:
```
🔧 Общая часть (без нормализации): 'Единая информационная система'
📋 Оригинальное значение: 'Единая информационная система управления финансами'
🔧 Общая часть: 'Единая информационная система'
🔒 Приватная часть: 'управления финансами'
🎯 Анонимизированный: 'Единая информационная система A1B2C3D4'
```

Это позволяет понять, как система разделила название и что именно будет заменено.